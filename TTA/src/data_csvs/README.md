# Omics CSV Files

This directory stores the omics CSV files and pathway metadata used by the survival pipeline.

The required CSV files follow the same organization and format as [MMP: Multimodal Prototyping for Cancer Survival Prediction](https://github.com/mahmoodlab/MMP). Download the corresponding RNA and metadata CSV files from the MMP repository and place them under this directory.
